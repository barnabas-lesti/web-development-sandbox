{
  "name": "@wds/root",
  "scripts": {
    "build": "turbo build",
    "clean": "git clean -Xfd",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "lint:fix": "turbo lint:fix",
    "prod": "turbo prod",
    "test": "turbo test"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "npm@10.7.0",
  "engines": {
    "node": ">=18.20.0",
    "npm": ">=10.7.0"
  },
  "devDependencies": {
    "turbo": "^2.0.14"
  }
}
