{
  "name": "web-development-sandbox",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "preview": "turbo preview",
    "lint": "turbo lint"
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "^3.8.1",
    "turbo": "^2.8.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "bun@0.0.0",
  "engines": {
    "node": "^24",
    "bun": "^1"
  }
}
