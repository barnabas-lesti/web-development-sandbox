{
  "name": "@wds/root",
  "scripts": {
    "build": "turbo build",
    "clean": "git clean -Xfd",
    "lint": "turbo lint",
    "lint:fix": "turbo lint:fix",
    "ng": "ng",
    "start": "turbo start",
    "start:ssr": "turbo start:ssr",
    "test": "turbo test"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "npm@10.7.0",
  "engines": {
    "node": ">=18.20.0",
    "npm": ">=10.7.0"
  },
  "devDependencies": {
    "@angular/cli": "^18.2.4",
    "turbo": "^2.0.14"
  }
}
