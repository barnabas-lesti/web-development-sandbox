{
  "name": "@wds/root",
  "scripts": {
    "build": "turbo build",
    "clean": "git clean -Xfd",
    "dev": "turbo dev",
    "dev:ssr": "turbo dev:ssr",
    "lint": "turbo lint --continue",
    "lint:fix": "turbo lint:fix --continue",
    "preview": "turbo preview"
  },
  "workspaces": [
    "apps/**",
    "packages/**"
  ],
  "packageManager": "npm@10.5.0",
  "engines": {
    "node": ">=18.20.0",
    "npm": ">=10.5.0"
  },
  "devDependencies": {
    "turbo": "^2.0.14"
  }
}
