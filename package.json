{
  "name": "@wds/root",
  "scripts": {
    "build": "turbo build --concurrency=100%",
    "build:prod": "turbo build:prod --concurrency=100%",
    "clean": "git clean -Xfd",
    "lint": "turbo lint --continue --concurrency=100%",
    "lint:fix": "turbo lint:fix --continue --concurrency=100%",
    "start": "turbo start",
    "start:prod": "turbo start:prod"
  },
  "workspaces": [
    "apps/**",
    "packages/**"
  ],
  "packageManager": "npm@10.5.0",
  "engines": {
    "node": ">=18.20.0",
    "npm": ">=10.5.0"
  },
  "devDependencies": {
    "turbo": "^2.0.14"
  }
}
