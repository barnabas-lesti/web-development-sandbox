import { type WdsExampleProps } from "./example.types";

/**
 * Example Mitosis generated component.
 */
export default function WdsExample(props: WdsExampleProps) {
  return <p>Hello {props.name || "Woarld"}!</p>;
}
