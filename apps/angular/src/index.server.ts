import { bootstrapApplication } from "@angular/platform-browser";
import { AppComponent, APP_SERVER_CONFIG } from "./app";

export default () => bootstrapApplication(AppComponent, APP_SERVER_CONFIG);
