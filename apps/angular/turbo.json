{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": [".angular/**", "dist/**"]
    },
    "dev": {
      "dependsOn": ["build"]
    },
    "prod": {
      "dependsOn": ["build"]
    }
  }
}
